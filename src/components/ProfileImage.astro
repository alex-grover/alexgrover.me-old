<img src="/profile.webp" alt="Profile picture" />

<style>
  div {
    width: var(--size);
    height: var(--size);
  }

  img {
    --size: 220px;

    width: var(--size);
    height: var(--size);
    object-fit: cover;
    border-radius: var(--r-full);
  }
</style>
